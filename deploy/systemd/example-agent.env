# Example per-agent notify listener settings for bridge-intake-watch@.service
BRIDGE_NOTIFY_PORT=8521
# Example: a read-only lifecycle event consumer command.
# Leave empty if you only want recipient intake/auto-ack behavior.
BRIDGE_NOTIFY_EVENT_COMMAND=
